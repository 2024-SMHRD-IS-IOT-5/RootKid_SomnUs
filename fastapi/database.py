# DB 연결 설정

# from motor.motor_asyncio import AsyncIOMotorClient
# from config import settings

# client = AsyncIOMotorClient(settings.DATABASE_URL)
# db = client["my_database"]  # 사용할 데이터베이스명
# users_collection = db["users"]  # 사용자 테이블 (컬렉션)
